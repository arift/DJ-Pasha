{
  "name": "dj-pasha",
  "version": "0.0.12",
  "description": "",
  "scripts": {
    "build": "npx rimraf ./build && tsc -p tsconfig.json --outDir build",
    "start": "node build/index.js --appId=$APP_ID --guildId=$GUILD_ID --discordToken=${DISCORD_TOKEN} --cookie=${COOKIE}"
  },
  "author": "Arif Topcu",
  "license": "MIT",
  "dependencies": {
    "@discordjs/voice": "^0.16.0",
    "@distube/ytdl-core": "^4.16.0",
    "date-fns": "^2.30.0",
    "discord-interactions": "^3.4.0",
    "discord.js": "^14.11.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "ffmpeg-static": "^5.1.0",
    "libsodium-wrappers": "^0.7.11",
    "sqlite3": "^5.1.7",
    "ytpl": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "eslint": "^8.46.0",
    "prettier": "^3.0.0",
    "typescript": "^5.7.3"
  }
}
